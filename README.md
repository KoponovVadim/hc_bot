
# Hangcha B2B Chat-Funnel Widget (Joomla 3)

Интерактивный чат-виджет для подбора промышленной техники Hangcha. Адаптирован для интеграции в Joomla 3. Виджет проводит пользователя через пошаговый сценарий подбора погрузчиков, складской техники, сервиса или консультации с менеджером.

## Описание

Виджет представляет собой чат-бот с ветвящейся логикой, который собирает требования пользователя и отправляет заявку на сервер. Поддерживает анимацию печати текста, валидацию ввода, AJAX-отправку данных и адаптивный дизайн.

### Основные возможности
- **Ветки сценария**: Подбор погрузчика, проверка наличия, складская техника, сервис/запчасти, вопрос менеджеру.
- **Динамические переходы**: Логика зависит от ответов пользователя.
- **Валидация**: Проверка email, телефона, чисел.
- **Отправка данных**: AJAX POST в JSON на PHP-обработчик.
- **UX**: Анимации, автоскролл, закрытие по Esc/клику вне виджета.

## Структура проекта

```
ханчабот/
├── hc-chat.js          # Основная логика чата (state machine, рендер, события)
├── hc-chat.css         # Стили виджета (адаптивный дизайн)
├── hc-chat.html        # HTML-разметка виджета
├── hc-chat.php         # PHP-обработчик для отправки данных (email/БД)
├── bot.html            # Тестовая страница для локального тестирования
└── README.md           # Этот файл
```

## Требования

- **Joomla 3.x** (или любой сайт с HTML/CSS/JS)
- **PHP 5.6+** для обработки форм
- **Браузеры**: Современные (Chrome, Firefox, Safari, Edge)
- **Зависимости**: Нет (чистый JS/CSS)

## Установка и интеграция

### 1. Копирование файлов
Скопируйте все файлы в папку вашего Joomla-сайта, например `/templates/your_template/hc-chat/`.

### 2. Подключение в шаблоне Joomla
В файле шаблона (например, `index.php`) добавьте:
```html
<link rel="stylesheet" href="/templates/your_template/hc-chat/hc-chat.css">
<script src="/templates/your_template/hc-chat/hc-chat.js"></script>
```

### 3. Вставка HTML-разметки
Вставьте разметку из `hc-chat.html` в нужное место шаблона (например, перед `</body>`):
```html
<!-- Кнопка открытия чата -->
<button id="chatOpenBtn" class="chat-open-btn">💬 Подобрать технику</button>

<!-- Виджет чата -->
<div id="chatWidget" class="chat-widget closed">
  <div class="chat-header">
    <span class="chat-title">Hangcha Chat</span>
    <button id="chatCloseBtn" class="chat-close-btn">✕</button>
  </div>
  <div id="chatMessages" class="chat-messages" tabindex="0"></div>
  <form id="chatInputArea" class="chat-input-area" style="display:none;">
    <input id="chatInput" type="text" placeholder="">
    <button id="chatSendBtn" type="submit" disabled>Отправить</button>
  </form>
</div>
```

### 4. Настройка пути к PHP
В `hc-chat.js` убедитесь, что путь к `hc-chat.php` корректный (строка 577):
```javascript
const response = await fetch('hc-chat.php', {
```

Если файл в другой папке, измените путь.

## Как работает

1. **Запуск**: Пользователь кликает на кнопку "💬 Подобрать технику".
2. **Сценарий**: Чат задаёт вопросы, пользователь выбирает опции или вводит данные.
3. **Валидация**: Ввод проверяется (email, телефон и т.д.).
4. **Отправка**: На финальном шаге данные отправляются AJAX на `hc-chat.php`.
5. **Завершение**: Показывается сводка и благодарность.

### Логика веток
- **Приветствие** → Выбор типа запроса.
- **Подбор погрузчика**: Двигатель → Место → Вес → Высота → Интенсивность → Город → Контакт → Финал.
- **Проверка наличия**: Модель → Подбор? → Контакт → Финал.
- **Складская техника**: Задачи → Параметры → Город → Контакт → Финал.
- **Сервис**: Вид услуги → Запрос → Город → Контакт → Финал.
- **Вопрос**: Вопрос → Контакт → Финал.

## Тестирование

Для локального тестирования откройте `bot.html` в браузере. Он включает все файлы и симулирует работу виджета без Joomla.

## Настройка и доработка

### PHP-обработчик (hc-chat.php)
Реализуйте логику обработки данных. Пример:
```php
<?php
header('Content-Type: application/json');
$data = json_decode(file_get_contents('php://input'), true);

// Сохранить в БД или отправить email
// Например:
mail('manager@company.com', 'Новая заявка', print_r($data, true));

echo json_encode(['status' => 'success']);
?>
```

### Кастомизация
- **Тексты**: Измените в массиве `steps` в `hc-chat.js`.
- **Стили**: Редактируйте `hc-chat.css` (CSS-переменные для тем).
- **Логика**: Добавляйте шаги в `steps`, обновляйте `next` функции.

### Безопасность
- Валидация на клиенте — базовая; всегда проверяйте данные на сервере.
- Защитите от спама (captcha, rate limiting).

## Поддержка и лицензия

- **Автор**: Разработано для компании Hangcha.
- **Лицензия**: Только для внутреннего использования компанией-заказчиком. Коммерческое распространение запрещено без согласования.
- **Контакт**: Для вопросов по интеграции обращайтесь к разработчику.

## Changelog
- v1.0: Базовая версия с ветками и AJAX.
